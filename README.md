1️⃣ What is the difference between var, let, and const?
let and const ar es6 features  and var is older  const cannotbe reassigned but let and var can let and are const block scoped and var Function Scope.

2️⃣ What is the spread operator (...)?
spread operator is es6 feture that help expands an array or object into indiviutual values it make  code cleaner by reducing the need for manual copying or looping.it massive use cloning merging and passing values.

3️⃣ What is the difference between map(), filter(), and forEach()?
map(),filter(), and forEach() , all are es6 features
map()- creates new arry by transforming new elemant.
filter()-creates new arry with elemants that match conditon.
forEach()- loops through  each element returns nothing.
4️⃣ What is an arrow function?
 An arrow  is a shoter way to write function in js .It was introduced in ES6.

5️⃣ What are template literals?
(``)template literals are  one of the most powerful features   in es6  that can embed value ${} we also can add multiline html with it.